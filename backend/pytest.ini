[pytest]
testpaths = tests
# Makes `app` importable regardless of where pytest is invoked from.
pythonpath = .
addopts = -q --strict-markers --tb=short
markers =
    integration: end-to-end workflow tests spanning several services
